export const LOCAL_KEY='cortical-v03';export const q=id=>document.getElementById(id);export const pad=n=>String(n).padStart(2,'0');export const iso=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;export function load(){try{return JSON.parse(localStorage.getItem(LOCAL_KEY)||localStorage.getItem('cortical-v02')||'[]')}catch{return[]}}export function store(a){localStorage.setItem(LOCAL_KEY,JSON.stringify(a))}export function sortItems(a){return [...a].sort((x,y)=>`${x.date||''} ${x.time||'99:99'}`.localeCompare(`${y.date||''} ${y.time||'99:99'}`))}export function renderItems(box,items){box.innerHTML='';if(!items.length){box.innerHTML='<div class="empty">No hay elementos guardados.</div>';return}sortItems(items).forEach(x=>{const e=document.createElement('div');e.className='item';e.innerHTML=`<div class="time">${x.time||'—'}</div><div><div class="title"></div><div class="meta"></div></div><div class="badge">${x.type||'Actividad'}</div>`;e.querySelector('.title').textContent=x.subject||x.raw||'Actividad';e.querySelector('.meta').textContent=[x.date||'',x.person&&x.person!=='—'?x.person:'',x.phone&&x.phone!=='—'?x.phone:''].filter(Boolean).join(' · ');box.appendChild(e)})}export function setDateLabel(){const el=q('dateLabel');if(!el)return;const d=new Date();el.textContent=d.toLocaleDateString('es-ES',{weekday:'long',day:'numeric',month:'long',year:'numeric'})}export function uniqueContacts(){const m=new Map();load().forEach(x=>{if((x.person&&x.person!=='—')||(x.phone&&x.phone!=='—')){const k=`${x.person||''}|${x.phone||''}`;if(!m.has(k))m.set(k,{person:x.person||'Sin nombre',phone:x.phone||'—'})}});return [...m.values()].sort((a,b)=>a.person.localeCompare(b.person,'es'))}